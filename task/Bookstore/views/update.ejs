<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Online Book Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        /* Main font and color scheme */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f2f5;
            color: #333;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        /* Centered form container with overlay effect */
        .form-overlay {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(8px);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 500px;
        }

        /* Title styling */
        .page-title {
            font-size: 1.8rem;
            font-weight: 600;
            color: #333;
            text-align: center;
            margin-bottom: 20px;
        }

        /* Label and input styling */
        .form-label {
            font-weight: bold;
            font-size: 1rem;
            color: #555;
        }

        .form-control,
        .form-select {
            padding: 12px;
            font-size: 1rem;
            border-radius: 8px;
            border: 1px solid #ddd;
            transition: all 0.3s ease;
        }

        .form-control:focus,
        .form-select:focus {
            border-color: #007bff;
            box-shadow: 0 0 8px rgba(0, 123, 255, 0.2);
        }

        /* Accent color for button */
        .btn-accent {
            background-color: #007bff;
            color: #fff;
            font-size: 1.1rem;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            padding: 12px;
            transition: background-color 0.3s, transform 0.3s;
        }

        .btn-accent:hover {
            background-color: #0056b3;
            transform: translateY(-3px);
        }
    </style>
</head>

<body>

    <!-- Form Container -->
    <div class="form-overlay">
        <h2 class="page-title">Add a New Book</h2>
        <form action="/editData/<%= storeData.id %>" method="post" class="row g-3">
            <div class="col-12">
                <label for="bookName" class="form-label">Book Title</label>
                <input type="text" name="bookName" id="bookName" class="form-control"
                    value="<%= storeData.bookName %>" placeholder="Enter Book Title" required>
            </div>
            <div class="col-12">
                <label for="bookCategory" class="form-label">Book Category</label>
                <select name="Bookcatagary" id="bookCategory" class="form-select" required>
                    <option value="">Select Category</option>
                    <option value="History" <%=storeData.Bookcatagary==="History" ? "selected" :"" %>>History</option>
                    <option value="Science fiction" <%=storeData.Bookcatagary==="Science fiction" ? "selected" :"" %>>Science Fiction</option>
                    <option value="Biography" <%=storeData.Bookcatagary==="Biography" ? "selected" :"" %>>Biography</option>
                    <option value="Mystery" <%=storeData.Bookcatagary==="Mystery" ? "selected" :"" %>>Mystery</option>
                    <option value="Business" <%=storeData.Bookcatagary==="Business" ? "selected" :"" %>>Business</option>
                </select>
            </div>
            <div class="col-12">
                <label for="bookPrice" class="form-label">Book Price</label>
                <input type="text" name="bookprice" id="bookPrice" value="<%= storeData.bookprice %>"
                    class="form-control" placeholder="Enter Price" required>
            </div>
            <div class="col-12 mt-4">
                <button type="submit" class="btn btn-accent w-100">Add Book</button>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>
